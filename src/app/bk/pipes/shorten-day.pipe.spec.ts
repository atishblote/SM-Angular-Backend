import { ShortenDayPipe } from './shorten-day.pipe';

describe('ShortenDayPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortenDayPipe();
    expect(pipe).toBeTruthy();
  });
});
