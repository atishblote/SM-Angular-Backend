import { OpeningTimeTextPipe } from './opening-time-text.pipe';

describe('OpeningTimeTextPipe', () => {
  it('create an instance', () => {
    const pipe = new OpeningTimeTextPipe();
    expect(pipe).toBeTruthy();
  });
});
